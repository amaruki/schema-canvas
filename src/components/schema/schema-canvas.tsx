/**
 * Schema Canvas - Main entry point for the refactored schema canvas
 * This file now serves as a simple wrapper around the modular component
 */

'use client';

import SchemaCanvasComponent from '../../features/schema/components/schema-canvas.component';

// Re-export for backward compatibility
export default SchemaCanvasComponent;